{
  "name": "nom-nom-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Nom Nom Tracker - Food tracking application",
  "scripts": {
    "lint": "npm run lint:backend && npm run lint:frontend",
    "lint:backend": "npm run --prefix backend lint",
    "lint:frontend": "npm run --prefix frontend lint",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "typecheck:backend": "npm run --prefix backend typecheck",
    "typecheck:frontend": "npm run --prefix frontend typecheck",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm run --prefix backend build",
    "build:frontend": "npm run --prefix frontend build",
    "test": "npm run --prefix frontend test -- --watchAll=false --passWithNoTests",
    "ci": "npm run lint && npm run typecheck && npm run build && npm run test"
  }
}
